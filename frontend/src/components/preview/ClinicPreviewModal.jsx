import ClinicBottomSheet from './ClinicBottomSheet';

/** @deprecated Use ClinicBottomSheet — kept for import compatibility */
export default function ClinicPreviewModal(props) {
  return <ClinicBottomSheet {...props} />;
}
